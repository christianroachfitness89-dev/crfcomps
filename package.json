{
  "name": "crf-comps",
  "version": "1.0.0",
  "description": "CRF Comps operations platform — static frontend + Vercel serverless functions for external integrations",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "googleapis": "^140.0.1",
    "stripe": "^16.5.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {},
  "engines": {
    "node": "22.x"
  }
}
